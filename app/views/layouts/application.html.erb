<!DOCTYPE html>
<html>
  <head>
    <title>Artists Only</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="header">
      <h1 class="title"> Artists Only </h1>
        <h3 class="subtitle"> "All my pictures are confused" </h3>
          <% if user_signed_in? %>
              Signed in as <%= current_user.username %>
              <%= button_to "Sign out", destroy_user_session_path, method: :delete %>
              <%= button_to "Your profile", user_path(current_user), method: :get %>
              <%= button_to "New post", new_post_path, method: :get %>
          <% end %>

          <% unless user_signed_in? %>
              <%= button_to "Sign up", new_user_registration_path %>
              <%= button_to "Sign in", new_user_session_path %>
          <% end %>
    </div>
      <% flash.each do |key, message| %>
        <div><%= message %></div>
      <% end %>
      <%= yield %>
  </body>
</html>
