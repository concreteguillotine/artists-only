<h2> Messages: </h2>
    <ul class="messages">
        <% @conversations.each do |c| %>
            <% if c.user == current_user %>
                <%= c.post.title %>
                <%= c.text %>
                <%= c.description %>
            <% end %>
        <% end %>
    </ul>
